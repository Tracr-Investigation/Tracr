import { Layout } from '../../components/Layout';

export const Parametres = () => {
  return (
    <Layout>
      <div className="p-8">
        <h1 className="text-3xl font-bold text-accent mb-4">Paramètres</h1>
        <p className="text-secondary">Configuration de l'application</p>
      </div>
    </Layout>
  );
};