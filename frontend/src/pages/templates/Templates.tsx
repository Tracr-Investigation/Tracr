import { Layout } from '../../components/Layout';

export const Templates = () => {
  return (
    <Layout>
      <div className="p-8">
        <h1 className="text-3xl font-bold text-text-default mb-4">Templates</h1>
        <p className="text-text-muted">Document templates</p>
      </div>
    </Layout>
  );
};
